package eum.movie.service;

import eum.movie.selData;
import javafx.scene.Parent;

public interface SelectMovieService3 {
	void nextPage3(Parent selectSession,selData sd);
	void previousPage3(Parent selectSession, selData sd);
}
