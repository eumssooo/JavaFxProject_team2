package eum.movie.service;

import javafx.scene.Parent;

public interface SelectMovieService3 {
	void selectSession(Parent selectSession);
	void nextPage3(Parent selectSession);
	void previousPage3(Parent selectSession);
}
